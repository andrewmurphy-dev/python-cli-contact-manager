so something i realized when doing this project ! 

1. 

we need to validate the digit string format for phone number 

we want it to be a digit string not a character string 


so we used the built in function isdigit()


for character in phone:
if not character.isdit():
print(character must be a digit)
return 




also i dont len(phone) <8:
print(too short) 


2. 

so the next is 
FastAPI reads the JSON body and creates the expense object from it, as long as correctstructure has matching fields like name and price.

@app.post("/expenses")
def add_expenses(expense: correctstructure):


3. 

we use try for the http request response 



4. 

do not forget that its called 

raise_for_status ! 



5.
also dont forget the error validation ,please rememebr this ! 

request.exceptions

4 times 

request.exceptions.ConnectionError:

request.exceptions.HTTPerror as error:

request.exceptions.RequestException

except ValueError 

